import 'package:flutter/material.dart';

class DeviceClips extends StatelessWidget {
  const DeviceClips({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(children: [Text("Clips")]);
  }
}
