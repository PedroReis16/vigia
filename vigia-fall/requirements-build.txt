# Dependências só para gerar o executável (make build / Docker).
# opencv-python-headless reforça o build headless (ultralytics puxa opencv-python GUI).
pyinstaller>=6.0
opencv-python-headless==4.12.0.88
