gpiozero==2.0.1
# Pi 5 / Linux only — no macOS (precisa de liblgpio + swig). Docker/build instala.
lgpio==0.2.2.0; sys_platform == "linux"
bless==0.3.0
getmac==0.8.2
cryptography==46.0.2
RPLCD==1.4.0
smbus2==0.5.0
python-dotenv==1.2.2

