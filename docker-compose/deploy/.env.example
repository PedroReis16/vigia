# Docker Hub
DOCKERHUB_USERNAME=pedroreis16

# Vigia API — versão em execução (atualizada pelo job `deploy` do
# workflow .github/workflows/service-release.yml ao final do pipeline).
VIGIA_API_IMAGE_TAG=latest

# Postgres (suas variáveis existentes)
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Keycloak
KEYCLOAK_DB_PASSWORD=
KEYCLOAK_ADMIN_USER=
KEYCLOAK_ADMIN_PASSWORD=