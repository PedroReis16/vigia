logLevel: info

# Control API used by on_unread hook to count remaining readers (localhost only).
api: yes
apiAddress: :9997

# Protocolos habilitados para teste de latencia
rtsp: yes
rtmp: yes
hls: yes
webrtc: yes

# Lista reduzida para evitar negociacoes desnecessarias
webrtcICEServers2:
  - url: stun:stun.l.google.com:19302

# Em Docker local (macOS), anunciar hosts explicitos evita candidatos ICE invalidos.
webrtcIPsFromInterfaces: false
webrtcAdditionalHosts:
  - vigia-deteccoes.duckdns.org
  - 54.157.40.38

# Fallback ICE por TCP para cenarios onde UDP local falha.
webrtcLocalTCPAddress: :8189

# Parametros com foco em baixa latencia no HLS (LL-HLS)
hlsVariant: lowLatency
hlsSegmentDuration: 1s
hlsPartDuration: 200ms
hlsSegmentCount: 6

paths:
  # Device RTMP publish: rtmp://…/live/{deviceId}
  # $G1 = device UUID capturado do path
  # NOTE: runOnUnDemand only runs with runOnDemand. Devices publish RTMP themselves,
  # so use runOnUnread + Control API (see /hooks/on_unread_stop_streaming.sh).
  "~^live/([0-9a-fA-F-]+)$":
    source: publisher
    runOnUnread: sh /hooks/on_unread_stop_streaming.sh
  all:
    source: publisher
